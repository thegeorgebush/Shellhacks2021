import React from 'react'
import { Paper } from '@material-ui/core';

const grossIncomeCard = (props) => {
    const { grossIncome } = props
    return (
        <Paper>
            <h1>0</h1>
        </Paper>
    )
}

export default grossIncomeCard
